{"prompt": "Medium 01 (iteration): This code is supposed to DISPLAY the numbers 1 through 5, one per loop. What is the main bug?", "code": "count ← 0\nREPEAT 5 TIMES {\n  count ← count + 1\n}\nDISPLAY(count)", "choices": ["The loop never CALLS DISPLAY inside the REPEAT block, so nothing is shown until the end", "REPEAT 5 TIMES should start count at 1 instead of 0", "count cannot be changed inside a REPEAT block", "REPEAT 5 TIMES is not valid AP CSP pseudocode"], "correctIndex": 0}
{"prompt": "Medium 02 (iteration): This loop is supposed to DISPLAY 1 2 3 4 5 and then stop. Why is this an infinite loop?", "code": "x ← 1\nREPEAT UNTIL (x > 5) {\n  DISPLAY(x)\n}", "choices": ["x is never changed inside the loop, so the condition x > 5 never becomes true", "REPEAT UNTIL must use = instead of >", "DISPLAY must come after the loop, not inside it", "x must start at 0 for REPEAT UNTIL to work"], "correctIndex": 0}
{"prompt": "Medium 03 (iteration): This loop is meant to add 3 + 2 + 1 into sum. What is the bug?", "code": "sum ← 0\nn ← 3\nREPEAT n TIMES {\n  sum ← sum + n\n  n ← n - 1\n}\nDISPLAY(sum)", "choices": ["Changing n inside REPEAT n TIMES is a bug; the loop count should not be modified inside the loop", "sum must start at 1 instead of 0", "REPEAT n TIMES must come before n is set", "DISPLAY(sum) must be inside the loop"], "correctIndex": 0}
{"prompt": "Medium 04 (iteration): This loop should display every element of numbers. What is the off-by-one bug?", "code": "index ← 1\nREPEAT UNTIL (index = LENGTH(numbers)) {\n  DISPLAY(numbers[index])\n  index ← index + 1\n}", "choices": ["The loop stops when index = LENGTH(numbers), so it never displays the last element", "index should start at 0 in AP CSP lists", "index should be decreased instead of increased", "LENGTH(numbers) cannot be used in a REPEAT UNTIL condition"], "correctIndex": 0}
{"prompt": "Medium 05 (iteration): This loop was supposed to run many times, but it stops immediately. What is wrong?", "code": "counter ← 1\nREPEAT UNTIL (counter ≥ 1) {\n  DISPLAY(\"Looping\")\n  counter ← counter + 1\n}", "choices": ["The condition counter ≥ 1 is already true before the loop, so the REPEAT UNTIL body never runs", "REPEAT UNTIL must always run at least once", "counter cannot be increased inside a REPEAT UNTIL loop", "The condition must use = instead of ≥"], "correctIndex": 0}
{"prompt": "Medium 06 (iteration): This FOR EACH loop is supposed to DISPLAY each value. Why is this a bug?", "code": "numbers ← [1, 2, 3]\nFOR EACH value IN numbers {\n  DISPLAY(numbers[index])\n}", "choices": ["It should DISPLAY(value); index is never defined in this loop", "FOR EACH cannot be used with lists in AP CSP pseudocode", "numbers must be sorted before a FOR EACH loop", "The braces { } are not allowed around the loop body"], "correctIndex": 0}
{"prompt": "Medium 07 (iteration): This code should sum all items in numbers. Why is it a logic bug?", "code": "total ← 0\nnumbers ← [1, 2, 3, 4]\nindex ← 1\nREPEAT 4 TIMES {\n  total ← total + numbers[index]\n}\nDISPLAY(total)", "choices": ["index is never changed inside the loop, so the same element is added every time", "REPEAT 4 TIMES cannot be used with lists", "total must start at 1 instead of 0", "numbers must have exactly 5 elements for REPEAT 4 TIMES to work"], "correctIndex": 0}
{"prompt": "Medium 08 (iteration): This code is supposed to DISPLAY the numbers 1 through 5, one per loop. What is the main bug?", "code": "count ← 0\nREPEAT 5 TIMES {\n  count ← count + 1\n}\nDISPLAY(count)", "choices": ["The loop never CALLS DISPLAY inside the REPEAT block, so nothing is shown until the end", "REPEAT 5 TIMES should start count at 1 instead of 0", "count cannot be changed inside a REPEAT block", "REPEAT 5 TIMES is not valid AP CSP pseudocode"], "correctIndex": 0}
{"prompt": "Medium 09 (iteration): This loop is supposed to DISPLAY 1 2 3 4 5 and then stop. Why is this an infinite loop?", "code": "x ← 1\nREPEAT UNTIL (x > 5) {\n  DISPLAY(x)\n}", "choices": ["x is never changed inside the loop, so the condition x > 5 never becomes true", "REPEAT UNTIL must use = instead of >", "DISPLAY must come after the loop, not inside it", "x must start at 0 for REPEAT UNTIL to work"], "correctIndex": 0}
{"prompt": "Medium 10 (iteration): This loop is meant to add 3 + 2 + 1 into sum. What is the bug?", "code": "sum ← 0\nn ← 3\nREPEAT n TIMES {\n  sum ← sum + n\n  n ← n - 1\n}\nDISPLAY(sum)", "choices": ["Changing n inside REPEAT n TIMES is a bug; the loop count should not be modified inside the loop", "sum must start at 1 instead of 0", "REPEAT n TIMES must come before n is set", "DISPLAY(sum) must be inside the loop"], "correctIndex": 0}
{"prompt": "Medium 11 (iteration): This loop should display every element of numbers. What is the off-by-one bug?", "code": "index ← 1\nREPEAT UNTIL (index = LENGTH(numbers)) {\n  DISPLAY(numbers[index])\n  index ← index + 1\n}", "choices": ["The loop stops when index = LENGTH(numbers), so it never displays the last element", "index should start at 0 in AP CSP lists", "index should be decreased instead of increased", "LENGTH(numbers) cannot be used in a REPEAT UNTIL condition"], "correctIndex": 0}
{"prompt": "Medium 12 (iteration): This loop was supposed to run many times, but it stops immediately. What is wrong?", "code": "counter ← 1\nREPEAT UNTIL (counter ≥ 1) {\n  DISPLAY(\"Looping\")\n  counter ← counter + 1\n}", "choices": ["The condition counter ≥ 1 is already true before the loop, so the REPEAT UNTIL body never runs", "REPEAT UNTIL must always run at least once", "counter cannot be increased inside a REPEAT UNTIL loop", "The condition must use = instead of ≥"], "correctIndex": 0}
{"prompt": "Medium 13 (iteration): This FOR EACH loop is supposed to DISPLAY each value. Why is this a bug?", "code": "numbers ← [1, 2, 3]\nFOR EACH value IN numbers {\n  DISPLAY(numbers[index])\n}", "choices": ["It should DISPLAY(value); index is never defined in this loop", "FOR EACH cannot be used with lists in AP CSP pseudocode", "numbers must be sorted before a FOR EACH loop", "The braces { } are not allowed around the loop body"], "correctIndex": 0}
{"prompt": "Medium 14 (iteration): This code should sum all items in numbers. Why is it a logic bug?", "code": "total ← 0\nnumbers ← [1, 2, 3, 4]\nindex ← 1\nREPEAT 4 TIMES {\n  total ← total + numbers[index]\n}\nDISPLAY(total)", "choices": ["index is never changed inside the loop, so the same element is added every time", "REPEAT 4 TIMES cannot be used with lists", "total must start at 1 instead of 0", "numbers must have exactly 5 elements for REPEAT 4 TIMES to work"], "correctIndex": 0}
{"prompt": "Medium 15 (iteration): This code is supposed to DISPLAY the numbers 1 through 5, one per loop. What is the main bug?", "code": "count ← 0\nREPEAT 5 TIMES {\n  count ← count + 1\n}\nDISPLAY(count)", "choices": ["The loop never CALLS DISPLAY inside the REPEAT block, so nothing is shown until the end", "REPEAT 5 TIMES should start count at 1 instead of 0", "count cannot be changed inside a REPEAT block", "REPEAT 5 TIMES is not valid AP CSP pseudocode"], "correctIndex": 0}
{"prompt": "Medium 16 (iteration): This loop is supposed to DISPLAY 1 2 3 4 5 and then stop. Why is this an infinite loop?", "code": "x ← 1\nREPEAT UNTIL (x > 5) {\n  DISPLAY(x)\n}", "choices": ["x is never changed inside the loop, so the condition x > 5 never becomes true", "REPEAT UNTIL must use = instead of >", "DISPLAY must come after the loop, not inside it", "x must start at 0 for REPEAT UNTIL to work"], "correctIndex": 0}
{"prompt": "Medium 17 (iteration): This loop is meant to add 3 + 2 + 1 into sum. What is the bug?", "code": "sum ← 0\nn ← 3\nREPEAT n TIMES {\n  sum ← sum + n\n  n ← n - 1\n}\nDISPLAY(sum)", "choices": ["Changing n inside REPEAT n TIMES is a bug; the loop count should not be modified inside the loop", "sum must start at 1 instead of 0", "REPEAT n TIMES must come before n is set", "DISPLAY(sum) must be inside the loop"], "correctIndex": 0}
{"prompt": "Medium 18 (iteration): This loop should display every element of numbers. What is the off-by-one bug?", "code": "index ← 1\nREPEAT UNTIL (index = LENGTH(numbers)) {\n  DISPLAY(numbers[index])\n  index ← index + 1\n}", "choices": ["The loop stops when index = LENGTH(numbers), so it never displays the last element", "index should start at 0 in AP CSP lists", "index should be decreased instead of increased", "LENGTH(numbers) cannot be used in a REPEAT UNTIL condition"], "correctIndex": 0}
{"prompt": "Medium 19 (iteration): This loop was supposed to run many times, but it stops immediately. What is wrong?", "code": "counter ← 1\nREPEAT UNTIL (counter ≥ 1) {\n  DISPLAY(\"Looping\")\n  counter ← counter + 1\n}", "choices": ["The condition counter ≥ 1 is already true before the loop, so the REPEAT UNTIL body never runs", "REPEAT UNTIL must always run at least once", "counter cannot be increased inside a REPEAT UNTIL loop", "The condition must use = instead of ≥"], "correctIndex": 0}
{"prompt": "Medium 20 (iteration): This FOR EACH loop is supposed to DISPLAY each value. Why is this a bug?", "code": "numbers ← [1, 2, 3]\nFOR EACH value IN numbers {\n  DISPLAY(numbers[index])\n}", "choices": ["It should DISPLAY(value); index is never defined in this loop", "FOR EACH cannot be used with lists in AP CSP pseudocode", "numbers must be sorted before a FOR EACH loop", "The braces { } are not allowed around the loop body"], "correctIndex": 0}
{"prompt": "Medium 21 (iteration): This code should sum all items in numbers. Why is it a logic bug?", "code": "total ← 0\nnumbers ← [1, 2, 3, 4]\nindex ← 1\nREPEAT 4 TIMES {\n  total ← total + numbers[index]\n}\nDISPLAY(total)", "choices": ["index is never changed inside the loop, so the same element is added every time", "REPEAT 4 TIMES cannot be used with lists", "total must start at 1 instead of 0", "numbers must have exactly 5 elements for REPEAT 4 TIMES to work"], "correctIndex": 0}
{"prompt": "Medium 22 (iteration): This code is supposed to DISPLAY the numbers 1 through 5, one per loop. What is the main bug?", "code": "count ← 0\nREPEAT 5 TIMES {\n  count ← count + 1\n}\nDISPLAY(count)", "choices": ["The loop never CALLS DISPLAY inside the REPEAT block, so nothing is shown until the end", "REPEAT 5 TIMES should start count at 1 instead of 0", "count cannot be changed inside a REPEAT block", "REPEAT 5 TIMES is not valid AP CSP pseudocode"], "correctIndex": 0}
{"prompt": "Medium 23 (iteration): This loop is supposed to DISPLAY 1 2 3 4 5 and then stop. Why is this an infinite loop?", "code": "x ← 1\nREPEAT UNTIL (x > 5) {\n  DISPLAY(x)\n}", "choices": ["x is never changed inside the loop, so the condition x > 5 never becomes true", "REPEAT UNTIL must use = instead of >", "DISPLAY must come after the loop, not inside it", "x must start at 0 for REPEAT UNTIL to work"], "correctIndex": 0}
{"prompt": "Medium 24 (iteration): This loop is meant to add 3 + 2 + 1 into sum. What is the bug?", "code": "sum ← 0\nn ← 3\nREPEAT n TIMES {\n  sum ← sum + n\n  n ← n - 1\n}\nDISPLAY(sum)", "choices": ["Changing n inside REPEAT n TIMES is a bug; the loop count should not be modified inside the loop", "sum must start at 1 instead of 0", "REPEAT n TIMES must come before n is set", "DISPLAY(sum) must be inside the loop"], "correctIndex": 0}
{"prompt": "Medium 25 (iteration): This loop should display every element of numbers. What is the off-by-one bug?", "code": "index ← 1\nREPEAT UNTIL (index = LENGTH(numbers)) {\n  DISPLAY(numbers[index])\n  index ← index + 1\n}", "choices": ["The loop stops when index = LENGTH(numbers), so it never displays the last element", "index should start at 0 in AP CSP lists", "index should be decreased instead of increased", "LENGTH(numbers) cannot be used in a REPEAT UNTIL condition"], "correctIndex": 0}
{"prompt": "Medium 26 (iteration): This loop was supposed to run many times, but it stops immediately. What is wrong?", "code": "counter ← 1\nREPEAT UNTIL (counter ≥ 1) {\n  DISPLAY(\"Looping\")\n  counter ← counter + 1\n}", "choices": ["The condition counter ≥ 1 is already true before the loop, so the REPEAT UNTIL body never runs", "REPEAT UNTIL must always run at least once", "counter cannot be increased inside a REPEAT UNTIL loop", "The condition must use = instead of ≥"], "correctIndex": 0}
{"prompt": "Medium 27 (iteration): This FOR EACH loop is supposed to DISPLAY each value. Why is this a bug?", "code": "numbers ← [1, 2, 3]\nFOR EACH value IN numbers {\n  DISPLAY(numbers[index])\n}", "choices": ["It should DISPLAY(value); index is never defined in this loop", "FOR EACH cannot be used with lists in AP CSP pseudocode", "numbers must be sorted before a FOR EACH loop", "The braces { } are not allowed around the loop body"], "correctIndex": 0}
{"prompt": "Medium 28 (iteration): This code should sum all items in numbers. Why is it a logic bug?", "code": "total ← 0\nnumbers ← [1, 2, 3, 4]\nindex ← 1\nREPEAT 4 TIMES {\n  total ← total + numbers[index]\n}\nDISPLAY(total)", "choices": ["index is never changed inside the loop, so the same element is added every time", "REPEAT 4 TIMES cannot be used with lists", "total must start at 1 instead of 0", "numbers must have exactly 5 elements for REPEAT 4 TIMES to work"], "correctIndex": 0}
{"prompt": "Medium 29 (iteration): This code is supposed to DISPLAY the numbers 1 through 5, one per loop. What is the main bug?", "code": "count ← 0\nREPEAT 5 TIMES {\n  count ← count + 1\n}\nDISPLAY(count)", "choices": ["The loop never CALLS DISPLAY inside the REPEAT block, so nothing is shown until the end", "REPEAT 5 TIMES should start count at 1 instead of 0", "count cannot be changed inside a REPEAT block", "REPEAT 5 TIMES is not valid AP CSP pseudocode"], "correctIndex": 0}
{"prompt": "Medium 30 (iteration): This loop is supposed to DISPLAY 1 2 3 4 5 and then stop. Why is this an infinite loop?", "code": "x ← 1\nREPEAT UNTIL (x > 5) {\n  DISPLAY(x)\n}", "choices": ["x is never changed inside the loop, so the condition x > 5 never becomes true", "REPEAT UNTIL must use = instead of >", "DISPLAY must come after the loop, not inside it", "x must start at 0 for REPEAT UNTIL to work"], "correctIndex": 0}
{"prompt": "Medium 31 (iteration): This loop is meant to add 3 + 2 + 1 into sum. What is the bug?", "code": "sum ← 0\nn ← 3\nREPEAT n TIMES {\n  sum ← sum + n\n  n ← n - 1\n}\nDISPLAY(sum)", "choices": ["Changing n inside REPEAT n TIMES is a bug; the loop count should not be modified inside the loop", "sum must start at 1 instead of 0", "REPEAT n TIMES must come before n is set", "DISPLAY(sum) must be inside the loop"], "correctIndex": 0}
{"prompt": "Medium 32 (iteration): This loop should display every element of numbers. What is the off-by-one bug?", "code": "index ← 1\nREPEAT UNTIL (index = LENGTH(numbers)) {\n  DISPLAY(numbers[index])\n  index ← index + 1\n}", "choices": ["The loop stops when index = LENGTH(numbers), so it never displays the last element", "index should start at 0 in AP CSP lists", "index should be decreased instead of increased", "LENGTH(numbers) cannot be used in a REPEAT UNTIL condition"], "correctIndex": 0}
{"prompt": "Medium 33 (iteration): This loop was supposed to run many times, but it stops immediately. What is wrong?", "code": "counter ← 1\nREPEAT UNTIL (counter ≥ 1) {\n  DISPLAY(\"Looping\")\n  counter ← counter + 1\n}", "choices": ["The condition counter ≥ 1 is already true before the loop, so the REPEAT UNTIL body never runs", "REPEAT UNTIL must always run at least once", "counter cannot be increased inside a REPEAT UNTIL loop", "The condition must use = instead of ≥"], "correctIndex": 0}
{"prompt": "Medium 34 (iteration): This FOR EACH loop is supposed to DISPLAY each value. Why is this a bug?", "code": "numbers ← [1, 2, 3]\nFOR EACH value IN numbers {\n  DISPLAY(numbers[index])\n}", "choices": ["It should DISPLAY(value); index is never defined in this loop", "FOR EACH cannot be used with lists in AP CSP pseudocode", "numbers must be sorted before a FOR EACH loop", "The braces { } are not allowed around the loop body"], "correctIndex": 0}
{"prompt": "Medium 35 (iteration): This code should sum all items in numbers. Why is it a logic bug?", "code": "total ← 0\nnumbers ← [1, 2, 3, 4]\nindex ← 1\nREPEAT 4 TIMES {\n  total ← total + numbers[index]\n}\nDISPLAY(total)", "choices": ["index is never changed inside the loop, so the same element is added every time", "REPEAT 4 TIMES cannot be used with lists", "total must start at 1 instead of 0", "numbers must have exactly 5 elements for REPEAT 4 TIMES to work"], "correctIndex": 0}
