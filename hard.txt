{"prompt": "Hard 01 (procedures + lists): This procedure should return the last element of numbers. What is the bug?", "code": "PROCEDURE getLast(numbers) {\n  RETURN(numbers[LENGTH(numbers) + 1])\n}\nvalues ← [3, 5, 7]\nresult ← getLast(values)\nDISPLAY(result)", "choices": ["It uses LENGTH(numbers) + 1, which is past the end of the list", "RETURN cannot be used inside a PROCEDURE in AP CSP pseudocode", "numbers must be sorted before calling getLast", "values must be a list of strings, not numbers"], "correctIndex": 0}
{"prompt": "Hard 02 (procedures + lists): addToEnd is supposed to APPEND value to the end of list. What is the indexing bug?", "code": "PROCEDURE addToEnd(list, value) {\n  INSERT(list, LENGTH(list), value)\n}\nnums ← [1, 2, 3]\naddToEnd(nums, 4)\nDISPLAY(nums)", "choices": ["INSERT at LENGTH(list) puts the new value before the last element; it should use LENGTH(list) + 1 or APPEND", "INSERT cannot be used with numbers", "list must be empty before using INSERT", "value must be a list, not a number"], "correctIndex": 0}
{"prompt": "Hard 03 (procedures + lists): This procedure should remove the first item from list. What is the bug?", "code": "PROCEDURE removeFirst(list) {\n  REMOVE(list, 0)\n}\nitems ← [\"a\", \"b\", \"c\"]\nremoveFirst(items)\nDISPLAY(items[1])", "choices": ["List indexing in AP CSP starts at 1, so index 0 is invalid", "REMOVE cannot be used inside a PROCEDURE", "items must contain numbers instead of strings", "removeFirst must RETURN the list instead of changing it"], "correctIndex": 0}
{"prompt": "Hard 04 (procedures + lists): doubleAll is meant to change nums so that every element is doubled. Why does this not work as intended?", "code": "PROCEDURE doubleAll(numbers) {\n  FOR EACH value IN numbers {\n    value ← value * 2\n  }\n}\nnums ← [1, 2, 3]\ndoubleAll(nums)\nDISPLAY(nums[1])", "choices": ["Changing value inside FOR EACH does not change the elements in the original list", "FOR EACH cannot be used in a PROCEDURE", "nums must be sorted before calling doubleAll", "The list must start at index 0 instead of 1"], "correctIndex": 0}
{"prompt": "Hard 05 (procedures + lists): sumList is supposed to return the sum of the list. What is the bug?", "code": "PROCEDURE sumList(numbers) {\n  total ← 0\n  FOR EACH num IN numbers {\n    total ← total + num\n  }\n}\nvalues ← [2, 4, 6]\nresult ← sumList(values)\nDISPLAY(result)", "choices": ["The procedure never RETURNS total, so result gets no value", "total must be a list, not a number", "FOR EACH cannot be used with the name num", "values must have exactly 4 elements"], "correctIndex": 0}
{"prompt": "Hard 06 (procedures + lists): average is supposed to compute the mean of the list. What loop bug prevents it from working?", "code": "PROCEDURE average(numbers) {\n  total ← 0\n  index ← 1\n  REPEAT UNTIL (index > LENGTH(numbers)) {\n    total ← total + numbers[index]\n  }\n  RETURN(total / LENGTH(numbers))\n}\nvalues ← [5, 7, 9]\nDISPLAY(average(values))", "choices": ["index is never changed inside the loop, so REPEAT UNTIL never stops", "LENGTH(numbers) cannot be used inside a PROCEDURE", "RETURN cannot use an expression with /", "numbers must start at index 0 instead of 1"], "correctIndex": 0}
{"prompt": "Hard 07 (procedures + lists): This procedure should return the first number greater than limit. What bug can cause it to skip valid answers?", "code": "PROCEDURE findFirstGreater(numbers, limit) {\n  index ← 1\n  REPEAT UNTIL (index > LENGTH(numbers)) {\n    IF (numbers[index] > limit) {\n      RETURN(numbers[index])\n    }\n    index ← index + 2\n  }\n  RETURN(-1)\n}\nvalues ← [3, 4, 5, 6]\nresult ← findFirstGreater(values, 4)\nDISPLAY(result)", "choices": ["index is increased by 2, so some elements in the list are never checked", "The IF condition must use ≥ instead of >", "RETURN(-1) must come before the loop", "LENGTH(values) must be used instead of LENGTH(numbers)"], "correctIndex": 0}
{"prompt": "Hard 08 (procedures + lists): appendTwice is meant to add value to the list two times. What is the bug in the call?", "code": "PROCEDURE appendTwice(list, value) {\n  APPEND(list, value)\n  APPEND(list, value)\n}\nnums ← []\nappendTwice(nums)\nDISPLAY(LENGTH(nums))", "choices": ["The call appendTwice(nums) is missing the value argument", "APPEND cannot be used on an empty list", "LENGTH(nums) cannot be displayed with DISPLAY", "list must be created inside the PROCEDURE, not outside"], "correctIndex": 0}
{"prompt": "Hard 09 (procedures + lists): This procedure should return the last element of numbers. What is the bug?", "code": "PROCEDURE getLast(numbers) {\n  RETURN(numbers[LENGTH(numbers) + 1])\n}\nvalues ← [3, 5, 7]\nresult ← getLast(values)\nDISPLAY(result)", "choices": ["It uses LENGTH(numbers) + 1, which is past the end of the list", "RETURN cannot be used inside a PROCEDURE in AP CSP pseudocode", "numbers must be sorted before calling getLast", "values must be a list of strings, not numbers"], "correctIndex": 0}
{"prompt": "Hard 10 (procedures + lists): addToEnd is supposed to APPEND value to the end of list. What is the indexing bug?", "code": "PROCEDURE addToEnd(list, value) {\n  INSERT(list, LENGTH(list), value)\n}\nnums ← [1, 2, 3]\naddToEnd(nums, 4)\nDISPLAY(nums)", "choices": ["INSERT at LENGTH(list) puts the new value before the last element; it should use LENGTH(list) + 1 or APPEND", "INSERT cannot be used with numbers", "list must be empty before using INSERT", "value must be a list, not a number"], "correctIndex": 0}
{"prompt": "Hard 11 (procedures + lists): This procedure should remove the first item from list. What is the bug?", "code": "PROCEDURE removeFirst(list) {\n  REMOVE(list, 0)\n}\nitems ← [\"a\", \"b\", \"c\"]\nremoveFirst(items)\nDISPLAY(items[1])", "choices": ["List indexing in AP CSP starts at 1, so index 0 is invalid", "REMOVE cannot be used inside a PROCEDURE", "items must contain numbers instead of strings", "removeFirst must RETURN the list instead of changing it"], "correctIndex": 0}
{"prompt": "Hard 12 (procedures + lists): doubleAll is meant to change nums so that every element is doubled. Why does this not work as intended?", "code": "PROCEDURE doubleAll(numbers) {\n  FOR EACH value IN numbers {\n    value ← value * 2\n  }\n}\nnums ← [1, 2, 3]\ndoubleAll(nums)\nDISPLAY(nums[1])", "choices": ["Changing value inside FOR EACH does not change the elements in the original list", "FOR EACH cannot be used in a PROCEDURE", "nums must be sorted before calling doubleAll", "The list must start at index 0 instead of 1"], "correctIndex": 0}
{"prompt": "Hard 13 (procedures + lists): sumList is supposed to return the sum of the list. What is the bug?", "code": "PROCEDURE sumList(numbers) {\n  total ← 0\n  FOR EACH num IN numbers {\n    total ← total + num\n  }\n}\nvalues ← [2, 4, 6]\nresult ← sumList(values)\nDISPLAY(result)", "choices": ["The procedure never RETURNS total, so result gets no value", "total must be a list, not a number", "FOR EACH cannot be used with the name num", "values must have exactly 4 elements"], "correctIndex": 0}
{"prompt": "Hard 14 (procedures + lists): average is supposed to compute the mean of the list. What loop bug prevents it from working?", "code": "PROCEDURE average(numbers) {\n  total ← 0\n  index ← 1\n  REPEAT UNTIL (index > LENGTH(numbers)) {\n    total ← total + numbers[index]\n  }\n  RETURN(total / LENGTH(numbers))\n}\nvalues ← [5, 7, 9]\nDISPLAY(average(values))", "choices": ["index is never changed inside the loop, so REPEAT UNTIL never stops", "LENGTH(numbers) cannot be used inside a PROCEDURE", "RETURN cannot use an expression with /", "numbers must start at index 0 instead of 1"], "correctIndex": 0}
{"prompt": "Hard 15 (procedures + lists): This procedure should return the first number greater than limit. What bug can cause it to skip valid answers?", "code": "PROCEDURE findFirstGreater(numbers, limit) {\n  index ← 1\n  REPEAT UNTIL (index > LENGTH(numbers)) {\n    IF (numbers[index] > limit) {\n      RETURN(numbers[index])\n    }\n    index ← index + 2\n  }\n  RETURN(-1)\n}\nvalues ← [3, 4, 5, 6]\nresult ← findFirstGreater(values, 4)\nDISPLAY(result)", "choices": ["index is increased by 2, so some elements in the list are never checked", "The IF condition must use ≥ instead of >", "RETURN(-1) must come before the loop", "LENGTH(values) must be used instead of LENGTH(numbers)"], "correctIndex": 0}
{"prompt": "Hard 16 (procedures + lists): appendTwice is meant to add value to the list two times. What is the bug in the call?", "code": "PROCEDURE appendTwice(list, value) {\n  APPEND(list, value)\n  APPEND(list, value)\n}\nnums ← []\nappendTwice(nums)\nDISPLAY(LENGTH(nums))", "choices": ["The call appendTwice(nums) is missing the value argument", "APPEND cannot be used on an empty list", "LENGTH(nums) cannot be displayed with DISPLAY", "list must be created inside the PROCEDURE, not outside"], "correctIndex": 0}
{"prompt": "Hard 17 (procedures + lists): This procedure should return the last element of numbers. What is the bug?", "code": "PROCEDURE getLast(numbers) {\n  RETURN(numbers[LENGTH(numbers) + 1])\n}\nvalues ← [3, 5, 7]\nresult ← getLast(values)\nDISPLAY(result)", "choices": ["It uses LENGTH(numbers) + 1, which is past the end of the list", "RETURN cannot be used inside a PROCEDURE in AP CSP pseudocode", "numbers must be sorted before calling getLast", "values must be a list of strings, not numbers"], "correctIndex": 0}
{"prompt": "Hard 18 (procedures + lists): addToEnd is supposed to APPEND value to the end of list. What is the indexing bug?", "code": "PROCEDURE addToEnd(list, value) {\n  INSERT(list, LENGTH(list), value)\n}\nnums ← [1, 2, 3]\naddToEnd(nums, 4)\nDISPLAY(nums)", "choices": ["INSERT at LENGTH(list) puts the new value before the last element; it should use LENGTH(list) + 1 or APPEND", "INSERT cannot be used with numbers", "list must be empty before using INSERT", "value must be a list, not a number"], "correctIndex": 0}
{"prompt": "Hard 19 (procedures + lists): This procedure should remove the first item from list. What is the bug?", "code": "PROCEDURE removeFirst(list) {\n  REMOVE(list, 0)\n}\nitems ← [\"a\", \"b\", \"c\"]\nremoveFirst(items)\nDISPLAY(items[1])", "choices": ["List indexing in AP CSP starts at 1, so index 0 is invalid", "REMOVE cannot be used inside a PROCEDURE", "items must contain numbers instead of strings", "removeFirst must RETURN the list instead of changing it"], "correctIndex": 0}
{"prompt": "Hard 20 (procedures + lists): doubleAll is meant to change nums so that every element is doubled. Why does this not work as intended?", "code": "PROCEDURE doubleAll(numbers) {\n  FOR EACH value IN numbers {\n    value ← value * 2\n  }\n}\nnums ← [1, 2, 3]\ndoubleAll(nums)\nDISPLAY(nums[1])", "choices": ["Changing value inside FOR EACH does not change the elements in the original list", "FOR EACH cannot be used in a PROCEDURE", "nums must be sorted before calling doubleAll", "The list must start at index 0 instead of 1"], "correctIndex": 0}
{"prompt": "Hard 21 (procedures + lists): sumList is supposed to return the sum of the list. What is the bug?", "code": "PROCEDURE sumList(numbers) {\n  total ← 0\n  FOR EACH num IN numbers {\n    total ← total + num\n  }\n}\nvalues ← [2, 4, 6]\nresult ← sumList(values)\nDISPLAY(result)", "choices": ["The procedure never RETURNS total, so result gets no value", "total must be a list, not a number", "FOR EACH cannot be used with the name num", "values must have exactly 4 elements"], "correctIndex": 0}
{"prompt": "Hard 22 (procedures + lists): average is supposed to compute the mean of the list. What loop bug prevents it from working?", "code": "PROCEDURE average(numbers) {\n  total ← 0\n  index ← 1\n  REPEAT UNTIL (index > LENGTH(numbers)) {\n    total ← total + numbers[index]\n  }\n  RETURN(total / LENGTH(numbers))\n}\nvalues ← [5, 7, 9]\nDISPLAY(average(values))", "choices": ["index is never changed inside the loop, so REPEAT UNTIL never stops", "LENGTH(numbers) cannot be used inside a PROCEDURE", "RETURN cannot use an expression with /", "numbers must start at index 0 instead of 1"], "correctIndex": 0}
{"prompt": "Hard 23 (procedures + lists): This procedure should return the first number greater than limit. What bug can cause it to skip valid answers?", "code": "PROCEDURE findFirstGreater(numbers, limit) {\n  index ← 1\n  REPEAT UNTIL (index > LENGTH(numbers)) {\n    IF (numbers[index] > limit) {\n      RETURN(numbers[index])\n    }\n    index ← index + 2\n  }\n  RETURN(-1)\n}\nvalues ← [3, 4, 5, 6]\nresult ← findFirstGreater(values, 4)\nDISPLAY(result)", "choices": ["index is increased by 2, so some elements in the list are never checked", "The IF condition must use ≥ instead of >", "RETURN(-1) must come before the loop", "LENGTH(values) must be used instead of LENGTH(numbers)"], "correctIndex": 0}
{"prompt": "Hard 24 (procedures + lists): appendTwice is meant to add value to the list two times. What is the bug in the call?", "code": "PROCEDURE appendTwice(list, value) {\n  APPEND(list, value)\n  APPEND(list, value)\n}\nnums ← []\nappendTwice(nums)\nDISPLAY(LENGTH(nums))", "choices": ["The call appendTwice(nums) is missing the value argument", "APPEND cannot be used on an empty list", "LENGTH(nums) cannot be displayed with DISPLAY", "list must be created inside the PROCEDURE, not outside"], "correctIndex": 0}
{"prompt": "Hard 25 (procedures + lists): This procedure should return the last element of numbers. What is the bug?", "code": "PROCEDURE getLast(numbers) {\n  RETURN(numbers[LENGTH(numbers) + 1])\n}\nvalues ← [3, 5, 7]\nresult ← getLast(values)\nDISPLAY(result)", "choices": ["It uses LENGTH(numbers) + 1, which is past the end of the list", "RETURN cannot be used inside a PROCEDURE in AP CSP pseudocode", "numbers must be sorted before calling getLast", "values must be a list of strings, not numbers"], "correctIndex": 0}
{"prompt": "Hard 26 (procedures + lists): addToEnd is supposed to APPEND value to the end of list. What is the indexing bug?", "code": "PROCEDURE addToEnd(list, value) {\n  INSERT(list, LENGTH(list), value)\n}\nnums ← [1, 2, 3]\naddToEnd(nums, 4)\nDISPLAY(nums)", "choices": ["INSERT at LENGTH(list) puts the new value before the last element; it should use LENGTH(list) + 1 or APPEND", "INSERT cannot be used with numbers", "list must be empty before using INSERT", "value must be a list, not a number"], "correctIndex": 0}
{"prompt": "Hard 27 (procedures + lists): This procedure should remove the first item from list. What is the bug?", "code": "PROCEDURE removeFirst(list) {\n  REMOVE(list, 0)\n}\nitems ← [\"a\", \"b\", \"c\"]\nremoveFirst(items)\nDISPLAY(items[1])", "choices": ["List indexing in AP CSP starts at 1, so index 0 is invalid", "REMOVE cannot be used inside a PROCEDURE", "items must contain numbers instead of strings", "removeFirst must RETURN the list instead of changing it"], "correctIndex": 0}
{"prompt": "Hard 28 (procedures + lists): doubleAll is meant to change nums so that every element is doubled. Why does this not work as intended?", "code": "PROCEDURE doubleAll(numbers) {\n  FOR EACH value IN numbers {\n    value ← value * 2\n  }\n}\nnums ← [1, 2, 3]\ndoubleAll(nums)\nDISPLAY(nums[1])", "choices": ["Changing value inside FOR EACH does not change the elements in the original list", "FOR EACH cannot be used in a PROCEDURE", "nums must be sorted before calling doubleAll", "The list must start at index 0 instead of 1"], "correctIndex": 0}
{"prompt": "Hard 29 (procedures + lists): sumList is supposed to return the sum of the list. What is the bug?", "code": "PROCEDURE sumList(numbers) {\n  total ← 0\n  FOR EACH num IN numbers {\n    total ← total + num\n  }\n}\nvalues ← [2, 4, 6]\nresult ← sumList(values)\nDISPLAY(result)", "choices": ["The procedure never RETURNS total, so result gets no value", "total must be a list, not a number", "FOR EACH cannot be used with the name num", "values must have exactly 4 elements"], "correctIndex": 0}
{"prompt": "Hard 30 (procedures + lists): average is supposed to compute the mean of the list. What loop bug prevents it from working?", "code": "PROCEDURE average(numbers) {\n  total ← 0\n  index ← 1\n  REPEAT UNTIL (index > LENGTH(numbers)) {\n    total ← total + numbers[index]\n  }\n  RETURN(total / LENGTH(numbers))\n}\nvalues ← [5, 7, 9]\nDISPLAY(average(values))", "choices": ["index is never changed inside the loop, so REPEAT UNTIL never stops", "LENGTH(numbers) cannot be used inside a PROCEDURE", "RETURN cannot use an expression with /", "numbers must start at index 0 instead of 1"], "correctIndex": 0}
{"prompt": "Hard 31 (procedures + lists): This procedure should return the first number greater than limit. What bug can cause it to skip valid answers?", "code": "PROCEDURE findFirstGreater(numbers, limit) {\n  index ← 1\n  REPEAT UNTIL (index > LENGTH(numbers)) {\n    IF (numbers[index] > limit) {\n      RETURN(numbers[index])\n    }\n    index ← index + 2\n  }\n  RETURN(-1)\n}\nvalues ← [3, 4, 5, 6]\nresult ← findFirstGreater(values, 4)\nDISPLAY(result)", "choices": ["index is increased by 2, so some elements in the list are never checked", "The IF condition must use ≥ instead of >", "RETURN(-1) must come before the loop", "LENGTH(values) must be used instead of LENGTH(numbers)"], "correctIndex": 0}
{"prompt": "Hard 32 (procedures + lists): appendTwice is meant to add value to the list two times. What is the bug in the call?", "code": "PROCEDURE appendTwice(list, value) {\n  APPEND(list, value)\n  APPEND(list, value)\n}\nnums ← []\nappendTwice(nums)\nDISPLAY(LENGTH(nums))", "choices": ["The call appendTwice(nums) is missing the value argument", "APPEND cannot be used on an empty list", "LENGTH(nums) cannot be displayed with DISPLAY", "list must be created inside the PROCEDURE, not outside"], "correctIndex": 0}
{"prompt": "Hard 33 (procedures + lists): This procedure should return the last element of numbers. What is the bug?", "code": "PROCEDURE getLast(numbers) {\n  RETURN(numbers[LENGTH(numbers) + 1])\n}\nvalues ← [3, 5, 7]\nresult ← getLast(values)\nDISPLAY(result)", "choices": ["It uses LENGTH(numbers) + 1, which is past the end of the list", "RETURN cannot be used inside a PROCEDURE in AP CSP pseudocode", "numbers must be sorted before calling getLast", "values must be a list of strings, not numbers"], "correctIndex": 0}
{"prompt": "Hard 34 (procedures + lists): addToEnd is supposed to APPEND value to the end of list. What is the indexing bug?", "code": "PROCEDURE addToEnd(list, value) {\n  INSERT(list, LENGTH(list), value)\n}\nnums ← [1, 2, 3]\naddToEnd(nums, 4)\nDISPLAY(nums)", "choices": ["INSERT at LENGTH(list) puts the new value before the last element; it should use LENGTH(list) + 1 or APPEND", "INSERT cannot be used with numbers", "list must be empty before using INSERT", "value must be a list, not a number"], "correctIndex": 0}
{"prompt": "Hard 35 (procedures + lists): This procedure should remove the first item from list. What is the bug?", "code": "PROCEDURE removeFirst(list) {\n  REMOVE(list, 0)\n}\nitems ← [\"a\", \"b\", \"c\"]\nremoveFirst(items)\nDISPLAY(items[1])", "choices": ["List indexing in AP CSP starts at 1, so index 0 is invalid", "REMOVE cannot be used inside a PROCEDURE", "items must contain numbers instead of strings", "removeFirst must RETURN the list instead of changing it"], "correctIndex": 0}
