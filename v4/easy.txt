{"prompt":"Easy 01 (selection): Why is this IF condition a bug?","code":"lives ← 3\nIF (lives ← 0) {\n  DISPLAY(\"Game over\")\n}","choices":["It uses assignment (←) instead of comparison (=) in the IF condition","The IF should check lives > 0 instead of lives = 0","The variable lives should be decreased before the IF instead of inside it","The DISPLAY message should show the number of lives instead of \"Game over\""],"correctIndex":0}
{"prompt":"Easy 02 (selection): This code should display \"Teen\" for ages 13–19. What is the bug?","code":"age ← 15\nIF (age ≥ 13 AND age ≥ 19) {\n  DISPLAY(\"Teen\")\n}","choices":["The second comparison should be age ≤ 19, not age ≥ 19","AND should be replaced with OR so the condition is easier to satisfy","The first comparison should be age > 13 instead of age ≥ 13","The IF should use only one comparison, not two"],"correctIndex":0}
{"prompt":"Easy 03 (selection): The program should display \"High score\" when score is at least 100. What is wrong?","code":"score ← 100\nIF (score > 100) {\n  DISPLAY(\"High score\")\n}","choices":["The condition excludes 100; it should use ≥ instead of >","The DISPLAY should be moved before the IF","The variable score must be reset to 0 after the IF","The IF condition should compare score to 0 instead of 100"],"correctIndex":0}
{"prompt":"Easy 04 (selection): This code should display \"Too cold\" when temp is below 32. What is the logic bug?","code":"temp ← 20\nIF (temp > 32) {\n  DISPLAY(\"Too cold\")\n}","choices":["The comparison is reversed; it should be temp < 32","The IF should check temp = 32, not temperatures below 32","The message \"Too cold\" should be displayed when temp is exactly 32 only","The variable temp should start at 0 instead of 20"],"correctIndex":0}
{"prompt":"Easy 05 (selection): Why is this comparison a bug?","code":"status ← \"OK\"\nIF (status = OK) {\n  DISPLAY(\"All good\")\n}","choices":["The string OK must be in quotes on both sides of =","The condition should compare status to a number, not a string","The IF should check status ≠ \"OK\" to detect a problem","status should be set inside the IF instead of before it"],"correctIndex":0}
{"prompt":"Easy 06 (selection): Only one grade should be displayed. What is the bug?","code":"score ← 95\nIF (score ≥ 90) {\n  DISPLAY(\"A\")\n}\nIF (score ≥ 80) {\n  DISPLAY(\"B\")\n}","choices":["Two separate IFs can both run; the second should be in an ELSE block","The second IF should check score < 90 instead of score ≥ 80","The first IF should use > instead of ≥ when checking 90","The IF statements must be in the opposite order to work correctly"],"correctIndex":0}
{"prompt":"Easy 07 (selection): This should display \"Wrong\" only when guess is NOT equal to secret. What is the bug?","code":"guess ← 5\nsecret ← 5\nIF (guess = secret) {\n  DISPLAY(\"Wrong\")\n}","choices":["The condition should use ≠ instead of = to detect a wrong guess","secret should be changed inside the IF before comparing","guess should be set to 0 before the comparison","The IF should check guess > secret instead of equality"],"correctIndex":0}
{"prompt":"Easy 08 (selection): The second DISPLAY should only run when the IF is true, but it always runs. Why?","code":"temperature ← 35\nIF (temperature > 30)\n  DISPLAY(\"Hot\")\nDISPLAY(\"Wear sunscreen\")","choices":["Only the first DISPLAY is controlled by the IF; braces { } are missing around both lines","The IF should compare temperature ≥ 30 instead of > 30","The second DISPLAY should be moved above the IF","The IF must use an ELSE before any additional DISPLAY"],"correctIndex":0}
{"prompt":"Easy 09 (selection): This should display \"Bring umbrella\" when it IS raining. What is the bug?","code":"isRaining ← TRUE\nIF (isRaining = FALSE) {\n  DISPLAY(\"Bring umbrella\")\n}","choices":["The condition is reversed; it should test isRaining = TRUE","The IF should compare isRaining to a string like \"TRUE\"","The IF should not use a Boolean at all, only numbers","The DISPLAY should be outside the IF so it always runs"],"correctIndex":0}
{"prompt":"Easy 10 (selection): Why is this IF–ELSE structure a syntax bug?","code":"score ← 90\nIF (score ≥ 90) {\n  grade ← \"A\"\n}\nELSE {\n  grade ← \"B\"\n}\nDISPLAY(grade)","choices":["The closing } for the IF block is missing before ELSE","The ELSE block must appear before the IF block","grade must be set to \"B\" before the IF runs","The DISPLAY must be inside the ELSE block, not after it"],"correctIndex":0}
{"prompt":"Easy 11 (selection): Why is this set of IF and ELSE blocks invalid?","code":"age ← 12\nIF (age < 13) {\n  DISPLAY(\"Child\")\n}\nELSE {\n  DISPLAY(\"Teen\")\n}\nELSE {\n  DISPLAY(\"Adult\")\n}","choices":["A single IF statement can have only one ELSE block","The first ELSE should be written as ELSE IF instead","The second ELSE should be moved above the IF","age must be at least 0 for ELSE to be used"],"correctIndex":0}
{"prompt":"Easy 12 (selection): This code should display \"Junior ticket\" only for ages between 10 and 13. Why is it wrong?","code":"age ← 16\nIF (age < 13 OR age > 10) {\n  DISPLAY(\"Junior ticket\")\n}","choices":["The OR makes the condition true for many ages; AND should be used instead","The comparisons should be age ≤ 13 and age ≥ 10","The IF should check only one boundary, not two","The condition should compare age to a Boolean instead of numbers"],"correctIndex":0}
{"prompt":"Easy 13 (selection): This should display \"Access granted\" only when BOTH are TRUE. What is the bug?","code":"usernameCorrect ← TRUE\npasswordCorrect ← FALSE\nIF (usernameCorrect OR passwordCorrect) {\n  DISPLAY(\"Access granted\")\n}","choices":["It should use AND instead of OR so both must be TRUE","The IF should compare usernameCorrect to passwordCorrect","The variables should be numbers instead of Booleans","The DISPLAY should be in an ELSE block instead"],"correctIndex":0}
{"prompt":"Easy 14 (selection): Why is this condition a logic bug?","code":"x ← 5\nIF (x = 3 OR 4) {\n  DISPLAY(\"Match\")\n}","choices":["The IF does not compare x to 4; it should be (x = 3 OR x = 4)","The IF should use AND instead of OR","The numbers 3 and 4 must be stored in variables before comparing","The condition should be written as x = 3 OR x = 4 = TRUE"],"correctIndex":0}
{"prompt":"Easy 15 (selection): This should display \"Fail\" when score is below 60. What is the bug?","code":"score ← 50\nIF (score ≥ 60) {\n  DISPLAY(\"Fail\")\n}","choices":["The comparison is backwards; it should be score < 60","The IF should use score ≤ 60 instead of ≥ 60","The message should say \"Pass\" instead of \"Fail\"","The IF should compare score to 0 instead of 60"],"correctIndex":0}
{"prompt":"Easy 16 (selection): The program should display \"Even\" when n is even. Why is this wrong?","code":"n ← 4\nIF (n MOD 2 = 1) {\n  DISPLAY(\"Even\")\n}","choices":["The condition checks for odd; it should use n MOD 2 = 0","The IF should check n MOD 2 ≠ 1 instead","The message should say \"Odd\" instead of \"Even\"","The MOD operator should divide by 10 instead of 2"],"correctIndex":0}
{"prompt":"Easy 17 (selection): This should display \"Game over\" when lives reach 0 or less. What is the bug?","code":"lives ← 0\nIF (lives > 0) {\n  DISPLAY(\"Game over\")\n}","choices":["The comparison is reversed; it should check lives ≤ 0","The IF should check lives = 0 only, not 0 or less","The variable lives should start at 3 instead of 0","The DISPLAY should run before the IF"],"correctIndex":0}
{"prompt":"Easy 18 (selection): Why is this use of NOT incorrect?","code":"isHungry ← TRUE\nIF (NOT isHungry = TRUE) {\n  DISPLAY(\"Eat now\")\n}","choices":["NOT should wrap the entire comparison: NOT(isHungry = TRUE)","NOT should be placed after TRUE, not before isHungry","The IF should compare isHungry to a number instead of TRUE","The condition should be isHungry = FALSE without NOT"],"correctIndex":0}
{"prompt":"Easy 19 (selection): Only one weather message should display. What is the bug?","code":"temp ← 90\nIF (temp ≥ 80) {\n  DISPLAY(\"Hot\")\n}\nELSE {\n  DISPLAY(\"Warm\")\n}\nDISPLAY(\"Weather report done\")","choices":["The last DISPLAY is outside the IF–ELSE and therefore always runs","The ELSE should come before the IF","The IF should use temp > 80 instead of temp ≥ 80","The code needs another ELSE for the final DISPLAY"],"correctIndex":0}
{"prompt":"Easy 20 (selection): This should display \"Inside\" only when x is between 1 and 10 exclusive. Why is it wrong?","code":"x ← 5\nIF (x > 1 OR x < 10) {\n  DISPLAY(\"Inside\")\n}","choices":["It should use AND instead of OR so both comparisons must be true","The comparisons should be x ≥ 1 and x ≤ 10","The IF must compare x to a Boolean instead of numbers","The IF should use only one comparison, not two"],"correctIndex":0}
{"prompt":"Easy 21 (selection): This should display \"Correct\" only when answer equals secret. What is the bug?","code":"answer ← 7\nsecret ← 7\nIF (answer ≠ secret) {\n  DISPLAY(\"Correct\")\n}","choices":["The condition is reversed; it should use = instead of ≠","The IF should check answer > secret","The program should display \"Wrong\" instead of \"Correct\"","secret should be set inside the IF instead of before it"],"correctIndex":0}
{"prompt":"Easy 22 (selection): Why is this IF statement invalid AP CSP pseudocode?","code":"num ← 3\nIF num > 0 {\n  DISPLAY(\"Positive\")\n}","choices":["The condition must be inside parentheses: IF (num > 0)","The IF should compare num to a Boolean, not a number","The comparison should be num ≥ 0 instead of num > 0","The DISPLAY should appear before the IF"],"correctIndex":0}
{"prompt":"Easy 23 (selection): This should display \"Free\" when cost is 0. Why might it fail?","code":"cost ← 0\nIF (cost) {\n  DISPLAY(\"Free\")\n}","choices":["The condition should explicitly compare cost to 0, like cost = 0","The IF should compare cost to TRUE instead of 0","The IF should check cost ≠ 0 to detect non-free items","The variable cost must be set to 1 instead of 0"],"correctIndex":0}
{"prompt":"Easy 24 (selection): Only a single grade should appear. What is wrong?","code":"score ← 75\nIF (score ≥ 60) {\n  DISPLAY(\"Pass\")\n}\nIF (score < 60) {\n  DISPLAY(\"Fail\")\n}","choices":["The second IF should be an ELSE so only one branch can run","The first IF should use score > 60 instead of ≥ 60","The IF conditions must be swapped to work correctly","score must be set to 0 before checking it"],"correctIndex":0}
{"prompt":"Easy 25 (selection): Why does this comparison fail to match the answer?","code":"answer ← \"YES\"\nIF (answer = \"yes\") {\n  DISPLAY(\"Confirmed\")\n}","choices":["The comparison is case-sensitive; \"YES\" is not equal to \"yes\"","The IF should check answer ≠ \"yes\"","The DISPLAY message must match the variable name","The variable answer should store a number instead of text"],"correctIndex":0}
{"prompt":"Easy 26 (selection): This should display \"Small\" when n is less than 10. What is the bug?","code":"n ← 5\nIF (n > 10) {\n  DISPLAY(\"Small\")\n}","choices":["The comparison is reversed; it should be n < 10","The IF should use n ≥ 10 instead of n > 10","The variable n should start at 10","The message \"Small\" should display when n ≥ 10 instead"],"correctIndex":0}
{"prompt":"Easy 27 (selection): Why is this grading logic incomplete?","code":"score ← 85\nIF (score ≥ 90) {\n  DISPLAY(\"A\")\n}\nELSE {\n  IF (score ≥ 80) {\n    DISPLAY(\"B\")\n  }\n}","choices":["Scores below 80 are never handled by this code","The inner IF should not be inside the ELSE","The ELSE must check score < 90 instead of score ≥ 80","The IF for \"B\" must use score > 90"],"correctIndex":0}
{"prompt":"Easy 28 (selection): This should display \"Unlocked\" when hasKey is TRUE. What is the bug?","code":"hasKey ← TRUE\nIF (hasKey = \"TRUE\") {\n  DISPLAY(\"Unlocked\")\n}","choices":["It compares to the string \"TRUE\" instead of the Boolean value TRUE","The IF should check hasKey = FALSE instead","The variable hasKey should start as FALSE","The DISPLAY should be outside the IF so it always runs"],"correctIndex":0}
{"prompt":"Easy 29 (selection): This should display \"Inside\" when x is between 1 and 100 inclusive. What is the bug?","code":"x ← 50\nIF (x > 1 AND x < 100) {\n  DISPLAY(\"Inside\")\n}","choices":["The endpoints 1 and 100 are excluded; it should use ≥ and ≤","The IF should use OR instead of AND","The comparisons should be reversed: x < 1 AND x > 100","The variable x should start at 0 instead of 50"],"correctIndex":0}
{"prompt":"Easy 30 (selection): Why is this age check backwards?","code":"age ← 10\nIF (age < 0 OR age > 200) {\n  DISPLAY(\"Valid age\")\n}","choices":["It displays \"Valid age\" only for impossible ages; the logic should be reversed","The IF should use AND instead of OR","The message should say \"Invalid age\" for these values","The variable age should be a string instead of a number"],"correctIndex":0}
{"prompt":"Easy 31 (selection): What is wrong with this IF–ELSE–ELSE sequence?","code":"a ← 4\nb ← 4\nIF (a = b) {\n  DISPLAY(\"Equal\")\n}\nELSE {\n  DISPLAY(\"Not equal\")\n}\nELSE {\n  DISPLAY(\"Error\")\n}","choices":["A single IF chain can have only one ELSE block","The second ELSE should come before the IF","The IF should use ≠ instead of = when comparing a and b","The DISPLAY in the first ELSE should be moved after the second ELSE"],"correctIndex":0}
{"prompt":"Easy 32 (selection): This code should display \"On\" when lightOn is TRUE. What is the bug?","code":"lightOn ← TRUE\nIF (lightOn ← TRUE) {\n  DISPLAY(\"On\")\n}","choices":["It uses assignment (←) in the condition instead of comparison (=)","The IF should check lightOn = FALSE instead","The message should say \"Off\" when lightOn is TRUE","The variable lightOn should be a number, not a Boolean"],"correctIndex":0}
{"prompt":"Easy 33 (selection): This should display \"Discount\" for customers 65 or older. What is wrong?","code":"age ← 65\nIF (age > 65) {\n  DISPLAY(\"Discount\")\n}","choices":["Age 65 is excluded; it should be age ≥ 65","The IF should check age < 65 instead","The message should display only when age = 65","The variable age should start at 0"],"correctIndex":0}
{"prompt":"Easy 34 (selection): Why might this IF block never run?","code":"count ← 0\nIF (count < 0) {\n  DISPLAY(\"Negative\")\n}","choices":["count is 0, so the condition is FALSE and the body never runs","The IF should use count ≥ 0 instead of count < 0","The variable count should be set to -1 before the IF","The DISPLAY should be moved outside the IF"],"correctIndex":0}
{"prompt":"Easy 35 (selection): This should display \"Match\" when color is \"red\" or \"blue\". What is the bug?","code":"color ← \"red\"\nIF (color = \"red\" AND color = \"blue\") {\n  DISPLAY(\"Match\")\n}","choices":["A value cannot be both \"red\" and \"blue\" at the same time; OR should be used instead of AND","The IF should compare color to a Boolean instead of strings","The code should use two separate IF statements, not one","The variable color should start as \"blue\" instead of \"red\""],"correctIndex":0}
{"prompt":"Medium 01 (selection): This code should display \"Pass\" when score is at least 60 and \"Fail\" otherwise. What is the bug?","code":"score ← 60\nIF (score > 60) {\n  DISPLAY(\"Pass\")\n}\nELSE {\n  DISPLAY(\"Fail\")\n}","choices":["A score of exactly 60 is treated as \"Fail\"; the condition should use ≥ instead of >","The ELSE should display \"Pass\" and the IF should display \"Fail\"","The IF should check score < 60 instead","The variable score must start at 0 for the logic to work"],"correctIndex":0}
{"prompt":"Medium 02 (selection): The code should display \"Child\" for age < 13, \"Teen\" for 13–17, and \"Adult\" otherwise. What is wrong?","code":"age ← 15\nIF (age < 13) {\n  DISPLAY(\"Child\")\n}\nELSE IF (age > 17) {\n  DISPLAY(\"Teen\")\n}\nELSE {\n  DISPLAY(\"Adult\")\n}","choices":["The Teen condition should be age ≥ 13 AND age ≤ 17, not age > 17","The Child condition should be age ≤ 13 instead of age < 13","The ELSE block should display \"Teen\" instead of \"Adult\"","The first IF and ELSE IF should be swapped"],"correctIndex":0}
{"prompt":"Medium 03 (selection): The robot should move only when both sensors are TRUE. What is the bug?","code":"frontClear ← TRUE\nbatteryOK ← FALSE\nIF (frontClear OR batteryOK) {\n  DISPLAY(\"Move\")\n}","choices":["The condition should use AND instead of OR so both must be TRUE","batteryOK should be set to TRUE inside the IF","The IF should compare frontClear to FALSE","The condition should use NOT(frontClear)"],"correctIndex":0}
{"prompt":"Medium 04 (selection): This code should display \"Inside\" only when x is between 10 and 20 inclusive. What is the logic bug?","code":"x ← 25\nIF (x ≥ 10 AND x ≥ 20) {\n  DISPLAY(\"Inside\")\n}","choices":["The second comparison should be x ≤ 20, not x ≥ 20","The first comparison should be x > 10, not x ≥ 10","The condition should use OR instead of AND","The IF should compare x to a Boolean instead of numbers"],"correctIndex":0}
{"prompt":"Medium 05 (selection): The program should display \"Alarm\" when temperature is above 100 OR smokeDetected is TRUE. What is wrong?","code":"temperature ← 105\nsmokeDetected ← FALSE\nIF (temperature > 100 AND smokeDetected = TRUE) {\n  DISPLAY(\"Alarm\")\n}","choices":["The condition should use OR instead of AND","smokeDetected should be compared to \"TRUE\" as a string","The IF should use temperature ≥ 100 instead of > 100","The DISPLAY should be in an ELSE block"],"correctIndex":0}
{"prompt":"Medium 06 (selection): The code should display \"Equal\" when a and b are both 0. What is the bug?","code":"a ← 0\nb ← 0\nIF (a = 0 OR b = 0) {\n  DISPLAY(\"Equal\")\n}","choices":["The condition should require both to be 0 using AND, not OR","The condition should be a ≠ 0 AND b ≠ 0","The code should compare a to b instead of 0","The IF should check a = 1 AND b = 1"],"correctIndex":0}
{"prompt":"Medium 07 (selection): The program should display \"Safe\" only when NOT emergency. What is the bug?","code":"emergency ← TRUE\nIF (NOT emergency = TRUE) {\n  DISPLAY(\"Safe\")\n}","choices":["NOT should wrap the whole comparison: NOT(emergency = TRUE)","The IF should check emergency = TRUE instead","The message should be displayed in an ELSE block","The variable emergency must be FALSE at the start"],"correctIndex":0}
{"prompt":"Medium 08 (selection): The code should display \"Even and positive\" only when n is even AND greater than 0. What is wrong?","code":"n ← -4\nIF (n MOD 2 = 0 OR n > 0) {\n  DISPLAY(\"Even and positive\")\n}","choices":["The condition should use AND instead of OR","The MOD comparison should check n MOD 2 = 1","The IF should compare n to a Boolean","The display message should change based on n"],"correctIndex":0}
{"prompt":"Medium 09 (selection): The door should unlock only when codeEntered is TRUE and attempts ≤ 3. What is the bug?","code":"codeEntered ← TRUE\nattempts ← 4\nIF (codeEntered = TRUE AND attempts < 3) {\n  DISPLAY(\"Unlocked\")\n}","choices":["The attempts comparison should allow 3 by using ≤ instead of <","The condition should use OR instead of AND","The variable attempts should start at 0","The IF should check attempts > 3 instead"],"correctIndex":0}
{"prompt":"Medium 10 (selection): This code should display \"Outside range\" when x is less than 1 OR greater than 10. What is wrong?","code":"x ← 0\nIF (x < 1 AND x > 10) {\n  DISPLAY(\"Outside range\")\n}","choices":["The condition should use OR instead of AND","The comparisons should be x ≤ 1 AND x ≥ 10","The IF should compare x to a Boolean","The IF should only check if x = 0"],"correctIndex":0}
{"prompt":"Medium 11 (selection): The program should display \"Zero\" when n is exactly 0. Why is this IF–ELSE chain incorrect?","code":"n ← 0\nIF (n < 0) {\n  DISPLAY(\"Negative\")\n}\nELSE IF (n > 0) {\n  DISPLAY(\"Positive\")\n}\nELSE IF (n ≠ 0) {\n  DISPLAY(\"Zero\")\n}","choices":["The final condition n ≠ 0 can never be TRUE when previous checks fail; it should check n = 0","The first IF should be n ≤ 0 instead of n < 0","The second IF should be n ≥ 0 instead of n > 0","The ELSE IF chain should start by checking n = 0"],"correctIndex":0}
{"prompt":"Medium 12 (selection): The program should display \"Discount\" if isStudent is TRUE OR age < 18. What is the bug?","code":"isStudent ← FALSE\nage ← 16\nIF (isStudent = TRUE AND age < 18) {\n  DISPLAY(\"Discount\")\n}","choices":["The condition should use OR instead of AND","The comparison age < 18 should be age ≤ 18","The IF should check age > 18","isStudent should be compared to \"TRUE\" instead of TRUE"],"correctIndex":0}
{"prompt":"Medium 13 (selection): This code should display \"Login blocked\" if attempts ≥ 3. What is wrong?","code":"attempts ← 3\nIF (attempts > 3) {\n  DISPLAY(\"Login blocked\")\n}","choices":["The comparison excludes 3; it should use ≥ instead of >","The IF should check attempts < 3","The variable attempts should be reset to 0 inside the IF","The message should be displayed in an ELSE block"],"correctIndex":0}
{"prompt":"Medium 14 (selection): The robot should turn left when leftClear is TRUE and rightClear is FALSE. What is the bug?","code":"leftClear ← TRUE\nrightClear ← FALSE\nIF (leftClear = TRUE OR rightClear = FALSE) {\n  DISPLAY(\"Turn left\")\n}","choices":["The condition should use AND instead of OR","The comparison rightClear = FALSE should be rightClear = TRUE","leftClear should be set to FALSE before the IF","The IF should not compare Booleans"],"correctIndex":0}
{"prompt":"Medium 15 (selection): The code should display \"Match\" when word is either \"yes\" or \"no\". What is wrong?","code":"word ← \"yes\"\nIF (word = \"yes\" AND word = \"no\") {\n  DISPLAY(\"Match\")\n}","choices":["A string cannot equal \"yes\" and \"no\" at the same time; OR should be used instead of AND","The IF should compare word to a Boolean","The variable word should be set to \"maybe\"","The IF should use ≠ instead of = for both comparisons"],"correctIndex":0}
